skills:
  - id: current_time
    specificity: high
    sentences:
      - id: query

  - id: current_date
    specificity: high
    sentences:
      - id: day
      - id: month
      - id: year


  - id: lyrics
    specificity: high
    sentences:
      - id: query
        captures:
          - id: song
            type: string

  - id: navigation
    specificity: high
    sentences:
      - id: query
        captures:
          - id: where
            type: string


  - id: open
    specificity: medium
    sentences:
      - id: query
        captures:
          - id: what
            type: string


  - id: search
    specificity: low
    sentences:
      - id: query
        captures:
          - id: what
            type: string

  - id: telephone
    specificity: low
    sentences:
      - id: dial
        captures:
          - id: who
            type: string
  - id: face_tracker
    specificity: high
    sentences:
      - id: start
      - id: stop

  - id: util_yes_no
    specificity: high
    sentences:
      - id: yes
      - id: no

  - id: weather
    specificity: high
    sentences:
      - id: current
        captures:
          - id: where
            type: string
